webdriver {
    driver = chrome
    autodownload = true
}

headless.mode = false

serenity {
    take.screenshots = AFTER_EACH_STEP
    project.name = "NTT Data E2E Test"
}

chrome.switches = "--start-maximized;--no-sandbox;--disable-dev-shm-usage;--disable-infobars"
